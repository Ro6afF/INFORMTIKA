#include <iostream>
#include <string>
#include <vector>
#include <stack>
std::vector<std::string> v;

int main()
{
	int n, m;
	std::cin >> n >> m;
	v.resize(n);
	for(int i = 0; i < n; i++)
	{
		std::cin >> v[i];
	}
	int p;
	std::cin >> p;
	for(int i = 0; i < p; i++)
	{
		std::string q;
		std::cin >> q;

	}
	return 0;
}
